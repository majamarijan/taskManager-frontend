import { createAvatar } from '@dicebear/avatars';
import * as style from '@dicebear/avatars-bottts-sprites';

export const svg = createAvatar(style, {
  seed: 'custom-seed',
  // ... and other options
});


